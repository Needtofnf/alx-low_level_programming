#include <stdio.h>

int main() {
  
  /**
 * main - Prints the alphabetic in lower and upper case
 *
 * Return: Always (Success)
 */
 char i;


    for(i='a'; i<='z'; i++)
       putchar(i);
 
 
    for(i='A'; i<='Z'; i++)
        putchar(i);
        
    return 0;
}
